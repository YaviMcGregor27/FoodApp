import { SeccionPendiente } from '../../../ui/SeccionPendiente';

export default function Recetas() {
  return (
    <SeccionPendiente
      titulo="Recetas"
      descripcion="Recetas que aprovechan lo que tienes, priorizando lo que caduca antes, con los ingredientes que faltan separados y adaptadas a tus restricciones."
      fase="F5"
    />
  );
}
