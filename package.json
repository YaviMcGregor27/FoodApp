{
  "name": "foodapp",
  "private": true,
  "description": "FoodApp: despensa, nutrición y objetivos corporales.",
  "workspaces": [
    "paquetes/*",
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
